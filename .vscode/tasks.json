{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "git push",
            "type": "shell",
            "command": "git config --global core.autocrlf false; git add .; ; git commit -m 'update'; git push",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}

git config --global --add safe.directory '%(prefix)///wsl.localhost/Ubuntu/home/earl/projects/pythonanywhere'; 